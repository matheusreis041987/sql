pragma foreign_keys=1; -- 1 para ativar a chave estrangeira e 0 para desativar

-- comando utilizado para encontrar todos os índices de uma tabela;
pragma index_list(tadega);
pragma index_list(produtor);